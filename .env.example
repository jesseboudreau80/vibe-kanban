# Vibe Kanban local development defaults
# Copy this file to .env or .env.local and override any values you need.

# Networking
HOST=127.0.0.1
FRONTEND_PORT=3000
BACKEND_PORT=3001
# If you prefer a single PORT value, leave BACKEND_PORT unset and set PORT to the desired frontend port.
# PORT=

# Analytics (optional)
POSTHOG_API_KEY=
POSTHOG_API_ENDPOINT=

# Shared API (optional)
VK_SHARED_API_BASE=

# MCP integration (optional)
# These values let you pre-provision a remote MCP server so agents inside Vibe Kanban can use it immediately.
VK_MCP_SERVER_URL=https://your-mcp-server.example.com
VK_MCP_TOKEN=replace-with-token
VK_MCP_CLIENT_NAME=vibe-kanban-local
VK_MCP_INSTANCE_ID=local-dev
VK_MCP_SERVER_NAME=external_mcp
VK_MCP_SERVER_DESCRIPTION=External MCP server configured via env
